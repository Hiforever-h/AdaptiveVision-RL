# AdaptiveVision-RL 项目计划

## 1. 项目定位与目标

本项目面向多模态算法与 Agent RL 实习能力展示，以 AdaptVision 的 Decoupled Turn Policy Optimization（DTPO）为核心，完成单卡条件下的方法复现与适配。

目标是训练一个按需获取视觉信息的 Agent：先观察低分辨率图片，自主选择直接回答，或请求原图中的局部高清区域后回答。在保持回答质量的同时，减少不必要的视觉输入与工具调用。

本阶段聚焦两条主线：

1. 复现 DTPO 对工具行为与回答行为的优化机制。
2. 基于 VisionThink-Smart 构造 **3,000 条训练子集**，通过离线视觉模型标注参考证据框，为在线训练提供可快速计算的裁剪奖励。

项目不以发表论文、提出新算法或复现原论文全部指标为目标。由于采用离线区域伪标签替代在线裁剪判分，并调整模型与训练规模，结果应表述为“DTPO 核心方法复现与单卡适配”，而非原论文完整实验复现。

## 2. 已确定的范围

| 项目 | 当前约定 |
| --- | --- |
| 算力 | 单张 A800 80G |
| 框架 | verl-agent |
| 底座 | 以 Qwen3-VL-4B 为计划基线，具体版本后续确定 |
| 参数更新 | LoRA |
| 核心算法 | DTPO；GRPO 是相关基础方法，不作为本阶段必须额外训练的对照 |
| 交互流程 | 低清观测后直接回答，或裁剪一次后回答；最多两轮模型输出 |
| 工具 | 从原始图片获取模型指定的局部高清区域 |
| 训练数据 | VisionThink-Smart 中筛选出的 3,000 条问答样本 |
| 轨迹来源 | 训练期间由当前策略在线生成 |
| 奖励 | 分开的答案／结果奖励与裁剪／工具奖励 |
| 裁剪判分 | 离线标注参考区域，训练时计算几何匹配奖励 |

暂不开展多次裁剪、多工具协作、复杂区域搜索、reasoning token 联合优化、大规模 SFT 数据构造或大量消融实验。

## 3. DTPO 复现主线

保留 DTPO 的核心思想：对工具与回答分别构建学习信号，并按论文定义处理优势估计和损失归一化。

- 结果奖励评价回答质量，并考虑必要的格式和调用约束。
- 工具奖励评价裁剪区域是否覆盖相关证据，以及是否引入过多无关区域。
- 按 DTPO 的定义组织两类优势与对应输出，而不是把所有奖励简单相加后统一分配给整条轨迹。
- 同时保留直接回答与工具调用两种路径，包括论文对直接回答路径的处理。
- 将 DTPO 迁移到 verl-agent 时，保持上述算法语义；具体接口与实现方案另行设计。

离线区域奖励是本项目对奖励来源的适配，不应被描述成论文原始奖励。它衡量的是与参考证据的几何一致性，不能完全替代对裁剪内容的语义判断。

## 4. 数据来源与划分

优先沿用 AdaptVision 官方使用的数据，不从零建立新的多模态问答数据集：

- 训练与调参来源：`Senqiao/VisionThink-Smart-Train`。
- 最终评测来源：`Senqiao/VisionThink-Smart-Val`。

| 集合 | 计划规模 | 用途 |
| --- | --- | --- |
| 标注先导集 | 首轮 20 条候选训练样本，按结果决定是否扩大到 100 条 | 检查离线标注是否可靠；合格样本可以计入最终训练子集 |
| 正式训练集 | **3,000 条样本**；按原始 `use_tool` 提示分层为低清即可回答 1,500 条、建议使用高清 1,500 条 | 在线生成轨迹并训练 DTPO |
| 独立调参集 | 暂定约 300 条 | 选择 checkpoint、校准奖励与观察训练状态 |
| 独立评测集 | 从 Smart-Val 中选取 500 条 | 冻结后的最终效果评估 |

3,000 条指不同的离线问答样本，不是 rollout 轨迹数。同一问题的多条训练轨迹由策略在线采样产生。

Train 采用 1:1 分层是为了给两类动作路径提供充足训练样本。独立调参集和最终评测集保留来源数据的自然比例，不据此重采样。

在标注和训练之前确定集合划分。同一图片或文档的相关样本不得跨集合，检查图片重复与近重复；固定抽样依据并保存样本清单。调参集和评测集不计入 3,000 条训练数据。

## 5. 数据构造主线

### 5.1 清洗与候选筛选

- 检查图片、问题和标准答案是否可用，保留最高可用分辨率的原图。
- 保留低清即可回答与获取细节可能有帮助的两类题目，避免只选择困难题或只选择必须调用工具的题。
- 优先选择局部高清细节可能有帮助的任务，例如局部文字、数值、物体属性或图表信息读取。裁剪图与低清全图共同使用，不要求裁剪图独立支持回答。
- 首轮试验保留答错、定位不可靠和全局类样本，记录失败类型，不依据教师输出反复重抽有利样本。
- 原数据中的工具使用标记仅作为抽样和分析参考，不视为当前底座必须遵守的动作标签。

### 5.2 离线标注参考证据框

使用具备视觉定位能力的教师模型，根据原图与问题标注参考区域。

标注对象是“与问题相关、适合补充高清细节的区域”。策略已持有低清全图，不要求局部框重复包含全局上下文。

正式标注默认使用 `glm-5.3-flash`，启用 thinking 并设置 `reasoning_effort=max`。输入原图和问题，不提供标准答案；使用像素坐标输出 `status`、`reference_boxes` 与 `answer_from_image`，不添加 `evidence_description`。样本是否通过首轮筛选以 `answer_from_image` 是否正确为依据；答案判断与定位诊断分别记录。

- 允许教师返回不确定或无法可靠标注，避免强制产生错误框。
- 默认保留一个参考框；确有不同区域都能独立支持回答时，可保留多个替代参考框。
- 不把“多个框可任选其一”与“必须同时看到多个区域”混为一谈；后者暂不纳入第一版重点。
- 统一记录坐标约定、原图尺寸、标注来源和质量检查状态。

参考框属于伪标签，不能直接视为人工真值。

### 5.3 离线质量检查

先完成 20 条标注试验，再决定是否扩大到 100 条及正式规模。

- 将参考框真正裁剪成图片，生成可视化框图，单独记录定位错误；不以缺少全局上下文判为失败。
- 对比 `answer_from_image` 与标准答案；先规范化文本／数值精确匹配，单位、别名等差异单独复核并记录。
- 本轮不调用模型进行“仅看裁剪图回答”的验证。
- 按答案正确口径导出先导候选，保留教师原始框，不把答对等同于框准确。发现的定位问题供后续决定是否调整标注方案。

按教师答案正确筛选会限制数据分布，应在报告中说明。最终评测不得根据教师标注结果或训练模型表现挑选有利样本。

## 6. 在线轨迹与奖励主线

离线保存原图、问题、答案及参考证据框；不预先固定模型的思考过程、工具动作或完整回答轨迹。

训练时的流程为：

1. 环境根据原图提供低清观测和问题。
2. 策略直接回答，或生成局部裁剪请求。
3. 若调用工具，环境从原图裁剪并返回局部高清观测，策略继续回答。
4. 分别计算结果奖励与工具奖励，按 DTPO 更新策略。

裁剪奖励以 **证据覆盖率与 IoU 的组合** 为起点：覆盖率关注必要证据是否保留，IoU 辅助约束无关区域。避免只用单个参考框的严格 IoU 误罚带有合理上下文的裁剪。具体权重和阈值留到后续实现阶段确定。

需要同时满足以下约束：

- 参考框存在，不意味着样本必须调用工具；低清能够回答时允许直接结束。
- 工具奖励、调用成本与面积约束需要协调，避免鼓励无条件裁剪或重复惩罚同一成本。
- 参考框、标准答案及标注质量信息只供环境和奖励计算使用，不进入策略模型输入。
- 裁剪必须来自保留的原图，不能用放大低清图代替。
- 消除在线裁剪判分不等于消除全部在线判分。答案奖励使用规则还是模型判分，需根据数据答案类型另行确定。

## 7. 最小评估与交付

不安排大规模消融。保留训练前底座与 DTPO 训练后的必要对照；固定低清／高清推理结果可作为成本与回答质量的参照。

重点记录：

- 回答正确率或对应任务分数。
- 工具调用率与无效裁剪比例。
- 全轨迹累计视觉 token 和端到端推理延迟。
- 结果奖励与工具奖励的训练变化。
- 简单题直接回答、细节题裁剪，以及失败案例的可视化。

几何匹配分数用于训练诊断，不能单独作为项目成功依据。验收以“DTPO 训练链路可复现、工具行为可解释、回答质量与成本有可核查结果”为准，不预先承诺达到论文指标或一定优于基线。

计划交付：

1. 固定的 3,000 条训练样本清单、独立调参与评测划分。
2. 离线参考证据框及质量检查记录。
3. 可复现的 DTPO 训练流程、配置与模型产物。
4. 简洁的效果报告，说明与原论文的差异、结果和局限。

## 8. 推进顺序

- [x] 确定 DTPO 与离线证据框数据构造主线。
- [x] 检查官方数据字段，完成 20 条先导标注；结果见 `reports/pilot20/RESULTS.md`。
- [x] 扫描全量数据并冻结 3,000/300/500 划分；Train 按原始 `use_tool` 提示 1:1 分层。
- [ ] 完成正式区域标注及质量检查。
- [ ] 确定答案判分方案与区域奖励口径。
- [ ] 在单卡上打通在线裁剪轨迹与 DTPO 更新。
- [ ] 完成训练、最小评估和结果整理。

脚本位于 `scripts/dataset_pilot/`，数据采用原图与 JSONL。均衡版正式划分位于 `data/visionthink_3000_300_500_balanced/`；正式区域标注、底座具体版本、训练参数与奖励权重仍待后续确定，尚未开始 RL 训练。

## 9. 参考资料

- [AdaptVision 论文](https://arxiv.org/abs/2512.03794)
- [AdaptVision 官方代码](https://github.com/AdaptVision/AdaptVision)
- [VisionThink-Smart-Train](https://huggingface.co/datasets/Senqiao/VisionThink-Smart-Train)
- [VisionThink-Smart-Val](https://huggingface.co/datasets/Senqiao/VisionThink-Smart-Val)
- [verl-agent](https://github.com/langfengQ/verl-agent)
