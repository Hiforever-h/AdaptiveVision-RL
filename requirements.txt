# Linux A800 training dependencies for Python 3.12.
# Source compatibility target: third_party/verl-agent.commit.
#
# flash-attn is intentionally installed separately after this file:
#   python -m pip install flash-attn==2.7.4.post1 --no-build-isolation --no-cache-dir
# The local verl-agent checkout is also installed separately with --no-deps.

# GPU rollout runtime. Its wheel resolves the matching PyTorch dependency.
vllm==0.11.0

# Versions constrained by the pinned verl-agent setup metadata.
transformers==4.57.3
ray[default]==2.50.0
tensordict==0.10.0
pyarrow>=19.0.0
packaging>=20.0

# verl-agent runtime dependencies.
accelerate
codetiming
datasets
dill
hydra-core
numpy
pandas
peft
pybind11
pylatexenc
torchdata
wandb
qwen-vl-utils[decord]

# AdaptiveVision-RL direct dependencies and CPU checks.
pillow
pytest
