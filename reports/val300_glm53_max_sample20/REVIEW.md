# Val 300 · GLM-5.3-Flash max 标注检查

## 全量运行

- 输入：均衡版正式数据的 Val 300；调用 `glm-5.3-flash`，thinking enabled，`reasoning_effort=max`。
- 坐标：教师返回原图整数像素 `xyxy`；脚本校验后同时保存像素框与确定性派生的 `[0,1]` 归一化框。
- 有效结构：292/300（97.3%）。其中 localized 272、global 17、uncertain 3。
- 失败：7 条模型输出校验失败，1 条接口 HTTP 400。成功返回但不合法的响应没有通过重试挑选更好的结果。
- 严格答案匹配：166/300；匹配且 localized、可进入区域奖励候选：154/300。
- API 返回用量合计 513,307 tokens；每条缓存请求平均 12.26 秒，中位数 8.76 秒。
- 272 个像素框转成归一化坐标后的最大反算误差为 `1.14e-13` 像素，属于浮点表示误差，不影响 coverage 或 IoU。

## 固定随机 20 条

使用 seed `20260922` 从完整 Val manifest 等概率无放回抽取 20 条。该随机样本碰巧包含 13 条 `use_tool=true`，高于 Val 总体的 31.7%，因此只用于定位质检，不用于估计总体准确率。

- 结构：18 条 localized、1 条 global、1 条 uncertain，20 条均有效。
- 语义答案：目视复核正确 18/20；严格字符串规则只匹配 14/20。
- 局部框：14/18 包含目标及足够上下文；2/18 上下文不完整；2/18 错过目标。
- 非局部状态：1 条 global 与 1 条 uncertain 均合理。
- 综合看，16/20 的区域处理可接受；正确回答不能替代定位检查，其中 2 条回答正确但框缺少必要信息。

主要失败类型：

1. 表格只框数据行，漏掉列标题。
2. 多区域计算题只框部分输入量。
3. 折线图选择了错误年份区间。
4. 当前严格答案匹配会拒绝带解释但数值／实体正确的答案，正式筛选前需要增加答案抽取或语义复核。

逐条判定见 `visual_review.jsonl`；可视化见 `index.html` 和 `contact-01.jpg` 至 `contact-05.jpg`。
