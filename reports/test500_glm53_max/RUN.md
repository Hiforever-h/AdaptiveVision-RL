# Test 500 · GLM-5.3-Flash max 标注记录

- 数据：均衡版正式数据中的 Test 500，来源为 Smart-Val。
- 请求：`glm-5.3-flash`，thinking enabled，`reasoning_effort=max`，原图整数像素坐标。
- 完整性：manifest、annotations 和 API cache 均为 500 条，sample ID 一一对应。
- 有效结构：495/500（99.0%）。其中 localized 491、global 2、uncertain 2。
- 失败：5 条模型响应包含非预期 JSON 字段，原始响应保留且没有重新采样。
- 严格答案匹配：239/500；严格匹配且 localized 的区域奖励候选为 237/500。该规则会漏掉“正确答案加解释”的响应，不能直接视为语义准确率。
- API 成功响应报告用量：800,147 tokens。
- 每条最终缓存请求平均 12.48 秒，中位数 9.85 秒。
- 491 个像素框到归一化框的最大反算误差为 `1.14e-13` 像素，不影响 coverage 或 IoU。

## 并发观察

首次按服务文档上限以内使用 40 并发。服务能够处理大部分请求，但当前客户端／网络链路产生约 75 条 SSL、读取超时或代理传输失败。将并发降至 10 后，仅重试未获得有效响应的记录，所有传输失败均恢复，已成功缓存的结果没有重复请求。

当前环境后续批量标注建议使用 10 并发；如果需要继续提高，先以 15 或 20 并发做小批量稳定性测试。
